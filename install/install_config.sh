#!/bin/bash

#通过注释来选择要安装的软件包
#软件包名前务必留空格

apt_list="${apt_list} vim"
apt_list="${apt_list} flex bison"
apt_list="${apt_list} git"
apt_list="${apt_list} nfs-kernel-server"
apt_list="${apt_list} openssh-server"
apt_list="${apt_list} rar"


